name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      GOMAXPROCS: "2"
    defaults:
      run:
        working-directory: glade
    steps:
      - uses: actions/checkout@v6
        with:
          path: glade
      - uses: actions/create-github-app-token@v3
        id: app-token
        with:
          client-id: ${{ vars.GLADE_APP_CLIENT_ID }}
          private-key: ${{ secrets.GLADE_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          repositories: |
            glade
            glade-tools
          permission-contents: read
      - name: Resolve glade-tools ref
        id: glade-tools-ref
        env:
          GLADE_TOOLS_REMOTE: https://x-access-token:${{ steps.app-token.outputs.token }}@github.com/glade-sh/glade-tools.git
          REQUESTED_REF: ${{ startsWith(github.ref, 'refs/tags/') && github.ref_name || 'main' }}
        run: scripts/resolve-sibling-ref.sh "$GLADE_TOOLS_REMOTE" "$REQUESTED_REF" main
      - uses: actions/checkout@v6
        with:
          repository: glade-sh/glade-tools
          path: glade-tools
          ref: ${{ steps.glade-tools-ref.outputs.ref }}
          token: ${{ steps.app-token.outputs.token }}
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache-dependency-path: glade/go.sum
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: |
            glade/third_party/lwc/package-lock.json
            glade/site/package-lock.json
      - run: npm ci --prefix third_party/lwc
      - run: npm ci --prefix site
      - run: npm test --prefix site
      - run: npm run build --prefix site
      - run: go vet ./...
      - run: scripts/ci-go-test.sh core
      - run: scripts/smoke.sh

  apextest:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    strategy:
      fail-fast: false
      matrix:
        shard: [0, 1]
    env:
      GOMAXPROCS: "2"
      GOMODCACHE: /tmp/go-mod-ci-apextest-${{ matrix.shard }}
      GOCACHE: /tmp/go-build-ci-apextest-${{ matrix.shard }}
      CI_APEXTEST_ARTIFACT_DIR: ci-artifacts/apextest-${{ matrix.shard }}
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version: "1.26.4"
          cache: false
      - name: Restore Apex module cache
        uses: actions/cache@v5
        with:
          path: ${{ env.GOMODCACHE }}
          key: go-mod-v1-1.26.4-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('go.sum') }}-ci-apextest-${{ matrix.shard }}-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            go-mod-v1-1.26.4-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('go.sum') }}-ci-apextest-${{ matrix.shard }}-
      - name: Restore Apex build cache
        uses: actions/cache@v5
        with:
          path: ${{ env.GOCACHE }}
          key: go-build-v1-1.26.4-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('go.sum') }}-ci-apextest-${{ matrix.shard }}-${{ github.sha }}-${{ github.run_id }}-${{ github.run_attempt }}
          restore-keys: |
            go-build-v1-1.26.4-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('go.sum') }}-ci-apextest-${{ matrix.shard }}-
      - name: Run Apex shard
        run: scripts/ci-go-test.sh apex-shard "${{ matrix.shard }}"
      - name: Upload Apex shard evidence
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: apex-shard-${{ matrix.shard }}
          path: |
            ci-artifacts/apextest-${{ matrix.shard }}/discovery.txt
            ci-artifacts/apextest-${{ matrix.shard }}/discovery-stderr.txt
            ci-artifacts/apextest-${{ matrix.shard }}/discovery-command.txt
            ci-artifacts/apextest-${{ matrix.shard }}/plan.json
            ci-artifacts/apextest-${{ matrix.shard }}/selected-shard.json
            ci-artifacts/apextest-${{ matrix.shard }}/events.json
            ci-artifacts/apextest-${{ matrix.shard }}/validation-summary.json
          if-no-files-found: error
