@IsTest
private class ArtifactConsumerTest {
    @IsTest
    static void resolvesArtifactDependencyTypeShape() {
        znu.DependencyService service;
        System.assertEquals(true, service == null);
    }
}
