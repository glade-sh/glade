@isTest
private class AccountServiceTest {
  @isTest static void testCreatesAccount() {
    System.assertEquals(2, 1 + 1);
  }

  @isTest static void testUpdatesAccount() {
    System.assertEquals(4, 2 + 2);
  }
}
