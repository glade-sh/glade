{
  "name": "core-string-split-nullable-unsupported",
  "evidence": [
    {
      "symbol": "String.split",
      "kind": "unsupported",
      "notes": "String.split shares the Pattern split fence for nullable delimiter regexes."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "String source = 'abc';\nsource.split('', -1);\n"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "String source = 'abc';\nsource.split('', -1);\n"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"String.split regexes that can match empty strings\""
    }
  }
}