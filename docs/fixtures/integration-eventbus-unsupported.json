{
  "name": "integration-eventbus-local-publish",
  "evidence": [{"symbol": "EventBus.publish", "kind": "exec", "notes": "Local platform event publish returns no-op success SaveResult records; delivery is not modeled."}],
  "source": [{"path": "anonymous.apex", "content": "Database.SaveResult result = EventBus.publish(new Account(Name = 'Acme')); System.assert(result.isSuccess());"}],
  "command": {"kind": "exec", "args": ["Database.SaveResult result = EventBus.publish(new Account(Name = 'Acme')); System.assert(result.isSuccess());"]},
  "expected": {"result": {"ok": true, "debug": null}}
}
