{
  "name": "core-numeric-stdlib-unsupported-double-format",
  "evidence": [
    {
      "symbol": "Double.format",
      "kind": "exec",
      "notes": "Locale/pattern numeric format overloads are not locally modeled and return a typed unsupported diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Double d = 7.25;\nd.format('en_US');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Double d = 7.25;\nd.format('en_US');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"numeric format locale/pattern overloads\""
    }
  }
}
