{
  "name": "core-url-current-request-unsupported",
  "evidence": [
    {
      "symbol": "URL.getCurrentRequestUrl",
      "kind": "exec",
      "notes": "Request-context URL helper is cloud-only in this local runtime slice."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "URL.getCurrentRequestUrl();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "URL.getCurrentRequestUrl();"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"URL.getCurrentRequestUrl\""
    }
  }
}
