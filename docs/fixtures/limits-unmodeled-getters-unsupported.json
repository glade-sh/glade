{
  "name": "limits-unmodeled-getters-unsupported",
  "evidence": [
    {
      "symbol": "Limits unmodeled getters",
      "kind": "exec",
      "notes": "Unmodeled Limits getters return a stable UnsupportedFeature diagnostic instead of fake counters."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Integer used = Limits.getSoslQueries();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Integer used = Limits.getSoslQueries();"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"Limits.getSoslQueries\""
    }
  }
}
