{
  "name": "http-static-resource-mock-unsupported",
  "evidence": [
    {
      "symbol": "StaticResourceCalloutMock",
      "kind": "exec",
      "notes": "Static-resource callout mocks are not locally modeled."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "StaticResourceCalloutMock mock = new StaticResourceCalloutMock();"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "StaticResourceCalloutMock mock = new StaticResourceCalloutMock();"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"StaticResourceCalloutMock local static resource callout mock surface\""
    }
  }
}