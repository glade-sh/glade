{
  "name": "core-numeric-stdlib-unsupported-format",
  "evidence": [
    {
      "symbol": "Integer.format",
      "kind": "exec",
      "notes": "Locale/pattern numeric format overloads are not locally modeled and return a typed unsupported diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "Integer i = 7;\ni.format('en_US');"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "Integer i = 7;\ni.format('en_US');"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"numeric format locale/pattern overloads\""
    }
  }
}
