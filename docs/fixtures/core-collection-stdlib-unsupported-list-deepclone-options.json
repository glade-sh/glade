{
  "name": "core-collection-stdlib-unsupported-list-deepclone-options",
  "evidence": [
    {
      "symbol": "List.deepClone",
      "kind": "exec",
      "notes": "SObject preserve-option overloads return a typed unsupported diagnostic."
    }
  ],
  "source": [
    {
      "path": "anonymous.apex",
      "content": "List<Account> accounts = new List<Account>{new Account(Id = '001B000001DVM9tIAH')};\naccounts.deepClone(true);"
    }
  ],
  "command": {
    "kind": "exec",
    "args": [
      "List<Account> accounts = new List<Account>{new Account(Id = '001B000001DVM9tIAH')};\naccounts.deepClone(true);"
    ]
  },
  "expected": {
    "error": {
      "type": "UnsupportedFeature",
      "message": "unsupported call \"List.deepClone with preserve options\""
    }
  }
}
